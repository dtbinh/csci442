Matt Bulands Project 1 readme

Files:
	1.	ping.py	<hostname> <TTL>
		Sends a ping to a hostname with an expected time to live, then reports back whether it was successful.

	2.	tracert.sh <hostname>
		Step-by-step sends a ping to a hostname. This results in a mapping depicting the server/routing path from host machine to the destination

Time spent: 3 hrs
